# Deps que corren en TU MAC (sin GPU). Instala con:
#   python -m venv venv && source venv/bin/activate
#   pip install -r requirements-mac.txt
#
# Nota: en Mac con Apple Silicon, mediapipe a veces necesita Python 3.10-3.11.
# Si mediapipe falla, usa un entorno con python 3.11.

numpy
opencv-python
ultralytics            # YOLO11 (detección de objetos)
mediapipe              # manos (21 keypoints)
mujoco==3.1.6          # Phase 2: simulador fisico del brazo Franka (wheel cp39 arm64).
                       # pybullet NO compila en py3.9 / Apple Silicon — migrado a MuJoCo.
flask                  # Phase 2: dashboard web en vivo (demo/live_dashboard.py)
requests               # cliente HTTP al pod
